services:
  resync:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "4000:4000"
    volumes:
      - resync-data:/app/backend/data
    environment:
      - PORT=4000
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  resync-data:
